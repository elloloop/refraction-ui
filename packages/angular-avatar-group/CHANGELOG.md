# @refraction-ui/angular-avatar-group

## 0.1.0

### Minor Changes

- Initial release of `angular-avatar-group` component.
  - `AvatarGroupComponent` (`re-avatar-group`) wrapping the headless `@refraction-ui/avatar-group` core.
  - Supports `users` (AvatarUser[]), `max`, and `size` inputs.
