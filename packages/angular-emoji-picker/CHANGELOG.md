# @refraction-ui/angular-emoji-picker

## 0.1.0

### Minor Changes

- Initial release of `angular-emoji-picker` component.
  - `EmojiPickerComponent` (`re-emoji-picker`) wrapping the headless `@refraction-ui/emoji-picker` core.
  - Supports search, category filtering, and emoji selection.
  - Fully accessible with ARIA roles and labels.
