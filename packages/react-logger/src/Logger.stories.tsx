// @refraction-ui/react-logger is a headless provider/hook package — no standalone UI.
export default { title: 'Utilities/Logger' }

export const Overview = {
  render: () => (
    <div className="max-w-md rounded-lg border border-border bg-card p-6 text-sm">
      <h3 className="mb-2 text-base font-semibold">@refraction-ui/react-logger</h3>
      <p className="text-muted-foreground">Headless structured logger — useLogger hook + provider. No standalone UI.</p>
    </div>
  ),
}
