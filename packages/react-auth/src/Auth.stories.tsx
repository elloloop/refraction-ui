// @refraction-ui/react-auth is a headless provider/hook package — no standalone UI.
export default { title: 'Components/Auth' }

export const Overview = {
  render: () => (
    <div className="max-w-md rounded-lg border border-border bg-card p-6 text-sm">
      <h3 className="mb-2 text-base font-semibold">@refraction-ui/react-auth</h3>
      <p className="text-muted-foreground">Headless auth provider + useAuth hook (login/signup/session). No standalone UI.</p>
    </div>
  ),
}
