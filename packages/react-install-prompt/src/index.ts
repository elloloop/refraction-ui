export { InstallPrompt, type InstallPromptProps } from './install-prompt.js'

// Re-export headless types for convenience
export {
  type InstallPromptState,
  type StorageAdapter,
  installPromptVariants,
} from '@refraction-ui/install-prompt'
