export { StickyNote, type StickyNoteProps, type StickyNoteColor } from './sticky-note.js'

// Re-export headless helpers so consumers don't reach into the private core.
export { STICKY_NOTE_COLORS, nextStickyColor } from '@refraction-ui/sticky-note'
