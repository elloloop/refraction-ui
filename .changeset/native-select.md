---
'@refraction-ui/react': minor
'@refraction-ui/astro': minor
---

Add `NativeSelect`: a styled native `<select>` (`size` `sm | default | lg`, `containerClassName`, every native attribute, ref to the select) for forms and filter bars that want platform pickers and native `change` semantics.
