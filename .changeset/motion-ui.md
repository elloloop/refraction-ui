---
'@refraction-ui/react': minor
'@refraction-ui/astro': minor
---

Add the Motion family: `PageTransition` (forward/back, vertical or side-by-side), `Reveal` (fold-in, sheet-in, scrim-in, handoff, celebrate, grow, nudge, icon-swap, num-pop), `Stagger`, `CheckDraw` and `useReducedMotion`, mirroring the named motion patterns with calm durations and easings that go inert under `prefers-reduced-motion`.
