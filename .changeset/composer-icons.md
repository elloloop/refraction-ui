---
"@refraction-ui/react": patch
---

Composer polish: replace the text/emoji toolbar labels with proper Lucide-style SVG icons (paperclip · B / I / code / link · quote / bulleted / numbered list), regroup with a vertical divider, swap the send button to a circular arrow-up (`disabled` state shown subtly). Cleaner default placeholder ("Send a message…"); the `/`, `@`, `:` trigger menus are unchanged behaviourally.
