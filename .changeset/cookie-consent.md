---
"@refraction-ui/react": minor
"@refraction-ui/astro": minor
---

Add CookieConsent — a headless, GDPR-style cookie-consent store (categories, accept all / reject all / save preferences, versioned persistence via a swappable storage adapter) with a React banner (`useCookieConsent` + `CookieConsent`, localStorage by default) and a server-rendered Astro `CookieConsent` banner that persists via localStorage and emits `rfr:cookie-consent`.
